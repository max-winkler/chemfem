!*.*
*.o
*.vtk
*.d
Make.inc

# Editor backups
*~
\#*#

# Generated by clangd and other tooling
compile_commands.json

# The linked test binaries. They carry no suffix, so they are listed by name rather
# than by pattern: an exception for everything with a suffix would also let the
# backups and the vtk output back in.
tests/*Test
tests/LocalRefinement
